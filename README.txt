Oscar Films clean runtime bundle

Included:
- index.html
- background-exact.png
- posters/

Removed as not needed for runtime:
- old enrichment scripts
- report JSON files
- template JSON files
- duplicate safe HTML files
- unused app.js
- macOS junk files

Note: Streaming data is not baked into this bundle. The app can still import an updated JSON at runtime, but the embedded dataset currently includes posters, synopsis, genres, and other core metadata only.
